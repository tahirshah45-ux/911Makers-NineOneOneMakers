/**
 * =============================================================================
 * LAYER 1: CORE (DOMAIN) - Interfaces Index
 * =============================================================================
 * Re-exports all repository interfaces.
 */

export type {
  IBusinessRepository,
  IIndustrySolutionRepository,
  IServiceRepository,
  ILeadRepository,
  IPricingRepository,
  ITestimonialRepository
} from './IBusinessRepository';